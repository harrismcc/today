services:
  web:
    command: >-
      pnpm db:migrate &&
      pnpm dev --host 0.0.0.0 --port "$PORT"
    health: /login
    portal:
      title: Today
      description: Create or use a passkey to sign in.
