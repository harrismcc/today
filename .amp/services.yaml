services:
  web:
    command: >-
      pnpm db:migrate &&
      pnpm dev --host 0.0.0.0 --port "$PORT"
    health: /login
    portal:
      title: Today
      description: Sign in or create an account with email and password.
